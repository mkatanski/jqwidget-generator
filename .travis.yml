language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jq-widget' ]; then cd .. && eval "mv $currentfolder generator-jq-widget" && cd generator-jq-widget; fi

